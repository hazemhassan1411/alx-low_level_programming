#include "dog.h"
/**
 * init_dog - it is a func
 * @d: it is avar
 * @name: it is avar
 * @age: it is avar
 * @owner: it is avar
 * Return: return d
 */


void init_dog(struct dog *d, char *name, float age, char *owner)
{
	if (d)
	{
		d->name = name
		d->owner = owner
		d->age = age
	}
}
