#include <stdio.h>

/**
 * main - write alphabet
 * Descripthion: prints the numbers
 * Return: always 0
*/

int main(void)
{
	int number = 0;

	while (number <= 9)
{
	printf("%i", number);
		number++;
}
putchar("\n");
return (0);
}
