#include <stdio.h>
/**
 * main - it is the main
 * @argc: it is avar
 * @argv: it is a var
 * Return: return 0
 */
int main(int argc, char *argv[]__attribute__((unused)))
{
	printf("%d\n", argc - 1);

	return (0);
}
