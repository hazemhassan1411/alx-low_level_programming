#include <stdio.h>
/**
 * main - it is a func
 * @argv: it is string
 * @argv: it is i 
 * Return: return the name
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
