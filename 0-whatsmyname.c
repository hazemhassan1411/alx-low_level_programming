#include <stdio.h>
/**
 * main - it is afunction
 * @argc: it is a var
 * @argv: t is a var
 * Return: return 0
 */

int main(int argc, const char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
